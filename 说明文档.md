# 舵轮步兵双板工程说明

本文档统一说明 STM32H723VET6 云台板和底盘板工程，包括硬件分配、软件架构、遥控映射、CAN2 数据交互、控制流程、构建烧录及 Ozone 调试方法。

## 1. 工程组成

| 工程 | 目录 | 固件输出 |
|---|---|---|
| 云台板 | `Gimbal/` | `.build/Debug/steerwheel_gimbal.elf` |
| 底盘板 | `Chassis/` | `.build/Debug/steerwheel_chassis.elf` |

两个工程分别构建、分别烧录，不需要通过构建参数选择板型。两个工程各自包含一份 ECF，可以独立移植；板间协议及安装位置宏必须保持一致。

## 2. 软件分层

```text
ECF/Algorithm   纯算法：PID、LQR、卡尔曼、姿态和舵轮运动学
ECF/Bsp         硬件抽象：CAN、USART、SPI、USB 等
ECF/Module      设备与机构：电机、BMI088、DR16、舵轮、发射、板间通信
ECF/App         可复用控制组件：云台、底盘、IMU、发射、视觉和安全

App/config      当前车辆的引脚、电机 ID、机械参数和 PID 初值
App/robot       创建设备对象，并把具体设备装入控制组件
App/task        FreeRTOS 周期调度
```

ECF 不应保存当前车型的电机 ID、机械尺寸和遥控操作习惯。当前 `ECF/App/app_command` 仍包含步兵遥控映射，后续可迁移到项目级 `App/command`。

## 3. 云台板

### 3.1 硬件分配

| 设备 | 总线 | 控制方式 |
|---|---|---|
| Pitch DM4310 | CAN1 | 位置/速度模式 |
| Yaw GM6020 | CAN2 | 位置外环、速度内环 |
| BMI088 | SPI | 姿态和角速度反馈 |
| 左右摩擦轮 M3508 | CAN1 | 速度闭环 |
| 可选拨弹盘 M3508 | CAN1 | 位置闭环 |
| 视觉 | USB 虚拟串口 | 目标角接收、IMU 数据发送 |
| 板间通信 | CAN2 | 与底盘交换关键数据 |

设备创建和 CAN 路由位于 `Gimbal/App/robot/robot_devices.c`，控制组件装配位于 `Gimbal/App/robot/robot_control.c`。

### 3.2 云台控制流程

```text
DR16 / CAN2远程DR16 / 视觉目标
                ↓
            app_command
                ↓
           app_exchange
                ↓
            app_gimbal
          ↙             ↘
 Pitch DM4310         Yaw GM6020

BMI088 → app_imu → 姿态反馈、视觉发送、板间状态
```

DR16 右摇杆产生 Yaw、Pitch 目标角速度，程序按周期积分为目标角。Pitch 目标限制在 `APP_GIMBAL_MIN_PITCH_RAD` 到 `APP_GIMBAL_MAX_PITCH_RAD`。

编码器位置闭环已经接入。工程提供 IMU/编码器反馈模式枚举，但当前电机目标仍直接写入电机位置控制器，所以 IMU 模式尚未形成完整的世界坐标位置闭环。LQR 算法库存在，但没有接入云台控制模式；当前实际使用 PID/电机内部位置控制链路。

### 3.3 视觉协议

视觉代码位于 `Gimbal/ECF/App/app_vision/app_vision.c`。

已实现：

- USB 虚拟串口收发；
- 固定长度数据帧；
- 帧头和 CRC8；
- 向视觉发送 IMU Pitch、Yaw；
- 接收视觉目标 Pitch、Yaw；
- 目标超时失效；
- 鼠标右键切换自动跟随。

视觉目标当前最终进入电机位置目标。完成真正的 IMU 闭环前，不建议把视觉模式用于高速实车跟踪。

## 4. 底盘板

### 4.1 硬件分配

| 设备 | 总线 | 控制方式 |
|---|---|---|
| 四个驱动 M3508 | CAN1 | 速度闭环 |
| 四个转向 GM6020 | CAN3 | 位置外环、速度内环 |
| 可选拨弹盘 M3508 | CAN1 | 位置闭环、堵转回退 |
| 板间通信 | CAN2 | 接收云台姿态、发送底盘状态 |

四个驱动电机和四个转向电机组合为四个 `module_swerve_t`，再交给 `alg_swerve` 计算每个轮组的目标速度和目标转角。

### 4.2 底盘控制流程

```text
DR16 / CAN2远程DR16
          ↓
      app_command
          ↓
     app_exchange
          ↓
      app_chassis
          ↓
      alg_swerve
          ↓
4 × module_swerve
   ↙             ↘
M3508速度环     GM6020位置/速度环
```

### 4.3 底盘模式

| 模式 | 行为 |
|---|---|
| 无力 | 禁用底盘电机输出 |
| 普通 | 按云台参考方向解释平移输入，不主动跟随 Yaw |
| 小陀螺 | 持续自旋，同时保持云台坐标系下的平移方向 |
| 跟随云台 | 根据云台相对 Yaw 产生底盘旋转速度 |

底盘运动学会根据 `reference_heading_rad` 将云台坐标系速度旋转到底盘坐标系。真正实现“小陀螺时头部世界方向不变”还依赖云台完成 IMU 世界坐标闭环。

### 4.4 静止自锁

平移和旋转指令均低于停止死区时，底盘进入 X 形自锁：

- 四个轮速目标为零；
- 四个转向目标朝向底盘中心；
- 外力推动时由转向位置闭环保持轮组姿态。

自锁代码位于 `ECF/App/app_chassis/app_chassis.c` 和 `ECF/Algorithm/alg_swerve/alg_swerve.c`。使用前必须正确标定四个 GM6020 的机械零位。

## 5. Command 映射

云台和底盘当前使用相同的 `ECF/App/app_command/app_command.c`。

### 5.1 摇杆

| 输入 | 通道 | 功能 |
|---|---:|---|
| 右摇杆左右 | `channel[0]` | 云台 Yaw 目标角速度 |
| 右摇杆上下 | `channel[1]` | 云台 Pitch 目标角速度 |
| 左摇杆左右 | `channel[2]` | 底盘 Y 方向速度 |
| 左摇杆上下 | `channel[3]` | 底盘 X 方向速度 |

默认最大值：

- Yaw：`3.5 rad/s`；
- Pitch：`2.0 rad/s`；
- 底盘平移：`3.0 m/s`；
- 小陀螺旋转：`6.0 rad/s`。

正负方向必须结合实车验证，必要时在项目配置或输入映射层修正。

### 5.2 三段开关

| 左开关 | 底盘行为 |
|---|---|
| 下 | 无力 |
| 中 | 普通或跟随，继续判断右开关 |
| 上 | 小陀螺 |

| 右开关 | 云台反馈选择 | 左开关为中时的底盘行为 |
|---|---|---|
| 上 | 编码器 | 普通 |
| 中 | IMU | 普通 |
| 下 | 编码器 | 跟随云台 |

左开关优先级高于右开关。当前无法同时选择“底盘跟随云台”和“云台 IMU 模式”，后续应重新分配操作逻辑。

### 5.3 拨轮和鼠标

| 输入 | 功能 |
|---|---|
| 拨轮 `>100` | 开启摩擦轮 |
| 拨轮 `>500` | 产生一次单发请求 |
| 鼠标右键 | 视觉自动模式和目标跟随 |
| 鼠标左键 | 当前未映射 |
| 键盘 | 已通过通信传输，但当前没有项目控制映射 |

摩擦轮目标速度当前固定为 `500 rad/s`。单发使用上升沿触发，拨轮需要退回后再次推过阈值才能请求下一发。

### 5.4 离线保护

DR16 本地离线或远程数据超时后：

- 底盘进入无力；
- 云台停止使能；
- 摩擦轮关闭；
- 不再请求发射；
- 摇杆输入按零处理。

## 6. CAN2 板间通信

协议位于两个工程的 `ECF/Module/module_board_comm`。

| 方向 | CAN ID 段 |
|---|---|
| 云台发送、底盘接收 | `0x500~0x506` |
| 底盘发送、云台接收 | `0x510~0x516` |

每块板只解析对端 ID 段，避免把本板发送的数据当作远端状态。

传输内容：

- DR16 四通道、拨轮、开关、鼠标和键盘；
- 云台 Yaw/Pitch、角速度、IMU 有效状态和电机在线状态；
- 底盘 X/Y/旋转速度、电机在线状态和自锁状态；
- 发射状态、摩擦轮到速状态和卡弹重试次数。

DR16 和云台数据采用多帧分片，接收端只有收到同一序列号的全部分片才提交新数据。各类数据具有独立离线计时。

## 7. DR16 与拨弹盘位置切换

两个工程的 `App/config/project_config.h` 都包含：

```c
#define PROJECT_DR16_LOCATION APP_DEVICE_LOCATION_CHASSIS
#define PROJECT_FEEDER_LOCATION APP_DEVICE_LOCATION_GIMBAL
```

可将位置改为 `APP_DEVICE_LOCATION_GIMBAL` 或 `APP_DEVICE_LOCATION_CHASSIS`。修改后必须同步修改另一个工程。

当前默认配置：

- DR16 在底盘板；
- 拨弹盘在云台板；
- 摩擦轮始终在云台板。

拨弹盘在底盘时，底盘通过 CAN2 获取云台摩擦轮到速状态，摩擦轮未到速时不允许送弹。

## 8. 发射和卡弹回退

发射状态机位于 `ECF/Module/module_shooter/module_shooter.c`：

```text
DISABLED → READY → FEEDING
                      ↓ 堵转
                  ROLLBACK
                   ↓     ↘ 超时或重试过多
                FEEDING   FAULT
```

堵转判断条件：

- 拨弹盘速度低于阈值；
- 电流高于阈值；
- 持续时间超过确认时间。

确认堵转后，拨弹目标反向移动指定角度；回退到位后重新尝试原目标。超过最大重试次数或回退超时后进入故障状态。

## 9. Ozone 调试

两个工程都保留全局符号：

- `robot`：设备对象和控制实例；
- `robot_observer`：初始化状态、失败步骤和更新计数。

### 9.1 通用初始化检查

| 结构体 | 重点字段 |
|---|---|
| `robot_observer` | `init_state`, `failed_step`, `last_status`, `module_error`, `initialized` |
| `robot.devices.board_comm` | 收发基址、`remote_online`, `gimbal_online`, `chassis_online`, `shooter_online` |

只有 `robot_observer.init_state == ROBOT_INIT_STATE_READY` 才表示实例初始化完成。

### 9.2 云台观察对象

| 对象 | 用途 |
|---|---|
| `robot.devices.pitch_motor` | Pitch 反馈、目标、在线和输出 |
| `robot.devices.yaw_motor` | Yaw 位置、速度、目标和串级控制器 |
| `robot.devices.bmi088` | 原始传感器数据和通信状态 |
| `robot.control.imu` | 姿态结果和有效状态 |
| `robot.control.gimbal` | 云台命令、反馈模式和锁定状态 |
| `robot.devices.shooter` | 摩擦轮到速、拨弹状态和卡弹次数 |

推荐调试顺序：反馈方向 → 输出限制 → 速度内环 → 位置外环 → Pitch 限位 → BMI088 轴向 → IMU闭环 → 视觉。

### 9.3 底盘观察对象

| 对象 | 用途 |
|---|---|
| `robot.devices.drive_motors[0..3]` | 四个驱动轮速度、目标和输出 |
| `robot.devices.steering_motors[0..3]` | 四个转向位置、速度和输出 |
| `robot.devices.swerve_modules[0..3]` | 单舵轮目标和反向优化结果 |
| `robot.devices.chassis_kinematics` | 轮组几何和目标分配 |
| `robot.control.chassis` | 当前底盘命令和模式 |

推荐调试顺序：逐个转向速度环 → 转向位置环 → 四个机械零位 → 驱动速度环 → 单轮组合 → 四轮平移 → 旋转 → 跟随 → 小陀螺 → 自锁。

